# Expo/React Native (Metro) requiere node_modules planos para resolver dependencias
# transitivas (peers de expo-router, @babel/runtime, etc.). node-linker=hoisted es la
# recomendación oficial de Expo para monorepos con pnpm.
node-linker=hoisted
